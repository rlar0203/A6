#include <iostream>
#include <string>

#include <cassert>
#include <cstring>
#define _USE_MATH_DEFINES
#include <cmath>
#include <iostream>



#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"

#define TINYOBJLOADER_IMPLEMENTATION
#include "tiny_obj_loader.h"
#define GLM_FORCE_RADIANS
#include </Programming\ assignment/GBIN/glm-master/glm/glm.hpp>
#include </glm/gtc/type_ptr.hpp>
#include "Image.h"

using namespace std;

//create function that generates rays given a camera 
vector<vec3> Ray_gen(vec3 camera,int height,int width){

vector<vec3> Rays;
   
    //calculates 45 degrees in radians
    FOV = M_PI*(45/180);

    //come up 
    
    //for loop which calculates the unit vector for each ray
    for (int i = 0; i < height*width; i++)
    {
        
        // need formula that calculates the angle needed to get the position for the ray initial starting position
    }
    

    return 


}
    
    
